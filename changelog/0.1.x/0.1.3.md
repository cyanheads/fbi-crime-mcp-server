---
summary: "Package metadata alignment, scripts migrated to bun run, Docker image published to GHCR"
breaking: false
security: false
---

# 0.1.3 — 2026-05-26

## Added

- `.github/FUNDING.yml` — GitHub Sponsors and Buy Me a Coffee funding links
- `author`, `funding`, `bugs`, `homepage`, `packageManager` fields in `package.json` matching ecosystem standard
- Docker image published to `ghcr.io/cyanheads/fbi-crime-mcp-server` (multi-arch: `linux/amd64`, `linux/arm64`)
- README badges: Docker (GHCR), MCP SDK, npm; install buttons for Claude Desktop (`.mcpb`), Cursor, and VS Code

## Changed

- `package.json` scripts migrated from `tsx scripts/…` to `bun run scripts/…`; `start:stdio`/`start:http` use `bun ./dist/index.js` instead of `node`; `test` uses `bunx vitest run`; `bundle` uses `bun run build`
- `files` array: removed `changelog/`, added `CLAUDE.md` (matches ecosystem standard)
- `tsx` removed from devDependencies (no longer needed after script migration)
